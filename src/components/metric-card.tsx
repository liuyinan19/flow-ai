// Legacy alias — re-exported from stat-card for any code paths still importing
// MetricCard. New components should import StatCard directly.
export { StatCard as MetricCard } from "@/components/stat-card";
